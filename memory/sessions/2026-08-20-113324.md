# Session (2026-08-20T11:33:24.000Z)

**Agent:** Cursor

Checkbox crops now use each row’s red tick strip as a search window, not as the crop. `SectionSpec` carries optional `field_id`; `_rows_in_leaf` and `split_row_text_and_ticks` copy it onto the green row and red tick. `apply_tick_windows` places one hollow square inside that strip per JSON `field_id`, else keeps the `snap_overlay` bbox. The pipeline gates on `_checkbox_grid_score` (keep gated if score ≥ snap − 0.02). Overlay still draws `sectioned`; handwriting stays on snap_overlay. Pytest `tests/test_normalization.py` 20 passed, including synthetic hollow ≥ 95% and a unit test that the crop sits on the square in the strip.

## Memory References

- referenced: block1-header-to-next-section-snap, block1-section-bar-gated-snap, nonverbal-precision-first, block1-section-match-samples
- created: block1-tick-window-gated-crops
