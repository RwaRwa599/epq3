# Session (2026-08-20T03:50:04.000Z)

**Agent:** Cursor

Snapped each main section onto its printed black header: top of the box = bar, height runs to the next bar in that column, width follows the bar (clipped to the column). Cyan subs map into the locked main. Tuned detection so top-of-page headers are not cut off and crease-split bars are merged before the height filter. Evaluated on five higher-quality scans (IMG_7596–IMG_7600): 13/13 mains header-hit on every sheet. Extra-ink vs the digital blank is still not started.

Also: inset each main from the header’s right (text) side so boxes no longer cover the next column’s tick boxes; HQ overlays now have 0 column X-overlap.

Also: cyan subs snap onto bold subheads inside each locked main (Health Check / Specialty Profile; Diabetes / Renal / Liver / Lipid Tests) and drop until the next subhead. All five HQ scans draw 6 subs.

Also: green row overlays inside each leaf (cyan sub, or main with no sub). One box per JSON test, snapped onto the printed line. On HQ scans, cyan overlay counts match JSON: 7 / 14 / 3 / 7 / 10 / 14.

Also: within each row, detect the test-name text box and subtract it; leftover is the tick box (orange text, red tick). HQ cyan groups: Health Check / Diabetes / Liver are 1 text + 1 tick per row on the clearer sheets.

## Memory References

- referenced: template-section-territories, block1-section-match-samples, decision-v0-default-v1-clinic
- created: block1-header-to-next-section-snap
